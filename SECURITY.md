# 安全政策 (Security Policy)

WorkBuddy Enterprise Edition 面向大型企业、国企、金融机构等**信息安全敏感行业**，安全是产品的核心诉求。我们重视社区报告的安全问题。

## 支持的版本

| 版本 | 安全更新 |
|---|---|
| `v0.1.x`（main 最新） | ✅ 支持 |

## 漏洞披露

**请勿通过公开 GitHub Issue 披露安全漏洞。**

请通过以下方式私下报告：

- **GitHub Security Advisories**（推荐）：在本仓库 `Security → Report a vulnerability` 提交私有漏洞报告。
- **邮箱**：`security@workbuddy.cn`（占位，请维护者替换为实际接收邮箱）

请尽量提供：

1. 漏洞描述与影响范围
2. 复现步骤 / PoC
3. 受影响的服务与版本
4. 建议的修复方向（如有）

## 响应时效

- **确认**：收到后 72 小时内确认并分配处理人
- **修复**：根据严重程度排期，高危优先
- **披露**：修复并发布后，经与报告人协商公开致谢（如同意）

## 企业部署安全基线建议

本平台为自托管软件，安全性同时取决于部署方式：

- **数据不出域**：推理 / 向量 / 业务库均运行在客户内网；如需接入 Claude / Codex 等外部模型，请通过企业合规网关并明确数据边界。
- **密钥管理**：生产环境 JWT 密钥、OIDC 凭据、模型 API Key 应使用 Vault 或密钥管理系统，**不要明文写在 `.env`**。
- **网络隔离**：`internal` 网络仅暴露 `gateway`（:8000）与前端（:3000）；数据库 / 向量库 / 推理引擎不对外。
- **审计**：开启 `audit-service`，定期导出审计日志留存（默认 180 天，可配 `AUDIT_RETENTION_DAYS`）。
- **合规路径**：等保三级、密评、关基保护条例相关控制项在路线图中规划（见 `ROADMAP.md`），正式投产前请由客户安全团队完成测评。

## 已知限制（v0.1.0）

- 内容审核、等保三级专用控制为**后续阶段**（当前 MVP 仅完成鉴权 / RBAC / 审计埋点）
- OIDC 单点登录为预留接口，需客户 IdP 配置后启用
- 真实 LLM 对话依赖 GPU 推理资源，MVP 验证环境未包含
